---
import Card from '../Card.astro'
import {PROFILE} from "../../content/profileData";
import { getCollection } from "astro:content";


const books = await getCollection('books');

console.log(books);
---

  <div class="h-full">
    <address class="mt-4 flex flex-col">
      <h2 class="text-gray-500">Contact Details</h2>
      <p>camiloplaza3@gmail.com</p>
      <p>🇨🇴 Bogotá D.C, Colombia 🇨🇴</p>
    </address>
    <div class="mt-4 flex w-fit flex-col">
      <h2 class="text-gray-500">Socials</h2>
      <ul class="list-inside list-disc">
        {books.map(
          (book) => (
            <li>
              <a href={`/books/${book.id}`} class="hover:font-bold underline">{book.data.title}</a>
              <span class="text-sm font-thin italic">- {book.data.author}</span>
            </li>
          )
        )}

      </ul>
    </div>
  </div>

